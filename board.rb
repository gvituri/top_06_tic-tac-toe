module Board

    @@board

    def update_board(board, move)
        #assign the last move to the board
    end

    def move_is_valid?(board, move)
        #test if the last move is valid
    end

    def move_wins?(board, move)
        #test if the move wins the game
        #returns false if not
        #returns true and the winning movement
    end
end